const
    expect = require('expect'),
    {describe, test} = require('mocha'),
    time = require('../src/time.js');

describe('fua.core.time', function () {

    test.only('develop', function () {
        console.log(time);
    });

});
